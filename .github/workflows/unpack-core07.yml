name: Unpack Core07 ZIP

on:
  push:
    paths:
      - 'OUTBASE_Core07_PREP_HUB_完成版.zip'
      - '.github/workflows/unpack-core07.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  unpack:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Unzip Core07 package
        run: |
          unzip -o "OUTBASE_Core07_PREP_HUB_完成版.zip" -d .
          rm -f "OUTBASE_Core07_PREP_HUB_完成版.zip"

      - name: Commit unpacked files
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "Core07 prep hub" || echo "No changes to commit"
          git push
