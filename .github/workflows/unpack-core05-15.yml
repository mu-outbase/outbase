name: OUTBASE Core05.15 NEXT Camp Plans unpack

on:
  push:
    branches:
      - main
    paths:
      - 'OUTBASE_MVPB_Core05_15_NextCampPlans.zip'
      - '.github/workflows/unpack-core05-15.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  unpack:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Confirm zip exists
        run: |
          set -e
          ls -la
          test -f OUTBASE_MVPB_Core05_15_NextCampPlans.zip

      - name: Unzip Core05.15 package
        run: |
          set -e
          rm -rf /tmp/outbase_core05_15
          mkdir -p /tmp/outbase_core05_15
          unzip -o OUTBASE_MVPB_Core05_15_NextCampPlans.zip -d /tmp/outbase_core05_15
          ls -la /tmp/outbase_core05_15
          cp -R /tmp/outbase_core05_15/. ./
          rm -f OUTBASE_MVPB_Core05_15_NextCampPlans.zip

      - name: Commit unpacked files
        run: |
          set -e
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Apply OUTBASE Core05.15 NEXT camp plans"
            git push
          fi
