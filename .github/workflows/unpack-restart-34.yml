name: OUTBASE Restart-34 Unpack

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "OUTBASE_Restart-34_複数プラン同時進行実行フェーズ統合LOCK_完成版.zip"
      - ".github/workflows/unpack-restart-34.yml"

permissions:
  contents: write

jobs:
  unpack:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Unpack OUTBASE Restart-34 ZIP
        run: |
          unzip -o "OUTBASE_Restart-34_複数プラン同時進行実行フェーズ統合LOCK_完成版.zip" -d .

      - name: Remove ZIP after unpack
        run: |
          rm -f "OUTBASE_Restart-34_複数プラン同時進行実行フェーズ統合LOCK_完成版.zip"

      - name: Commit unpacked files
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add index.html style.css src/app.js manifest.json service-worker.js
          git add -u
          git commit -m "Apply OUTBASE Restart-34 multi plan execution files" || echo "No changes to commit"
          git push
