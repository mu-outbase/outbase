name: OUTBASE Restart-25 Unpack

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "OUTBASE_Restart-25_日常UI簡素化LOCK_完成版.zip"
      - ".github/workflows/unpack-restart-25.yml"

permissions:
  contents: write

jobs:
  unpack:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Unpack OUTBASE Restart-25 ZIP
        run: |
          unzip -o "OUTBASE_Restart-25_日常UI簡素化LOCK_完成版.zip" -d .

      - name: Remove ZIP after unpack
        run: |
          rm -f "OUTBASE_Restart-25_日常UI簡素化LOCK_完成版.zip"

      - name: Commit unpacked files
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add index.html style.css src/app.js manifest.json service-worker.js
          git add -u
          git commit -m "Apply OUTBASE Restart-25 daily UI files" || echo "No changes to commit"
          git push
