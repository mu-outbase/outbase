name: OUTBASE Core05.16 Camp Plan Unified unpack

on:
  push:
    branches:
      - main
    paths:
      - 'OUTBASE_MVPB_Core05_16_CampPlanUnified.zip'
      - '.github/workflows/unpack-core05-16.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  unpack:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Confirm zip exists
        run: |
          set -e
          ls -la
          test -f OUTBASE_MVPB_Core05_16_CampPlanUnified.zip

      - name: Unzip Core05.16 package
        run: |
          set -e
          rm -rf /tmp/outbase_core05_16
          mkdir -p /tmp/outbase_core05_16
          unzip -o OUTBASE_MVPB_Core05_16_CampPlanUnified.zip -d /tmp/outbase_core05_16
          ls -la /tmp/outbase_core05_16
          cp -R /tmp/outbase_core05_16/. ./
          rm -f OUTBASE_MVPB_Core05_16_CampPlanUnified.zip

      - name: Commit unpacked files
        run: |
          set -e
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Apply OUTBASE Core05.16 camp plan unified"
            git push
          fi
