name: OUTBASE Restart-18 Unpack

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "OUTBASE_Restart-18_UI構造リセット技術採用ガードレールLOCK_完成版.zip"
      - ".github/workflows/unpack-restart-18.yml"

permissions:
  contents: write

jobs:
  unpack:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Unpack OUTBASE Restart-18 ZIP
        run: |
          unzip -o "OUTBASE_Restart-18_UI構造リセット技術採用ガードレールLOCK_完成版.zip" -d .

      - name: Remove ZIP after unpack
        run: |
          rm -f "OUTBASE_Restart-18_UI構造リセット技術採用ガードレールLOCK_完成版.zip"

      - name: Commit unpacked files
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add index.html style.css src/app.js manifest.json service-worker.js
          git add -u
          git commit -m "Apply OUTBASE Restart-18 UI reset files" || echo "No changes to commit"
          git push
