name: Unpack Core08-F1 ZIP

on:
  push:
    paths:
      - 'OUTBASE_Core08_F1_FULL_MODE_EXPERIENCE_完成版.zip'
      - '.github/workflows/unpack-core08-f1.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  unpack:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Unzip Core08-F1 package
        run: |
          unzip -o "OUTBASE_Core08_F1_FULL_MODE_EXPERIENCE_完成版.zip" -d .
          rm -f "OUTBASE_Core08_F1_FULL_MODE_EXPERIENCE_完成版.zip"
      - name: Commit unpacked files
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "Core08-F1 full mode experience" || echo "No changes to commit"
          git push
