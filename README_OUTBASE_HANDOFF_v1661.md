# OUTBASE v166.1 引継ぎ

最優先：MASTER v166.1、BOOT v7.1、STATUSLOCK v166.1、GitHub main最新。

正式デザインは通常時NORTH、active／paused時TRAIL LENS。

v166 Android実機で正式デザインは表示されたが、更新時の旧FIELD03ちらつきとシェル遷移の重さを確認。v166.1でfast shell、route別read model、persistent shell、routine View Transition停止を実装した。

FIELD03 app.js、GPS、地図、メディア、DB schema、保存データは変更していない。GitHub反映・Android再確認前は完成扱いにしない。
