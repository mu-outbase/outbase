
(()=> {
  'use strict';
  const VERSION='REBUILD16_DESIGN_CORE';
  const KEY='outbase_rebuild16_state';
  const app=document.getElementById('app');
  const clone=o=>JSON.parse(JSON.stringify(o));
  const pad=n=>String(n).padStart(2,'0');
  const today=()=>new Date().toISOString().slice(0,10);
  const iso=d=>`${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`;
  const fmt=s=>s?`${Number(s.slice(5,7))}/${Number(s.slice(8,10))}`:'未定';
  const timeFmt=d=>`${pad(d.getHours())}:${pad(d.getMinutes())}`;
  const nowTime=()=>new Date().toLocaleTimeString('ja-JP',{hour:'2-digit',minute:'2-digit'});
  const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const uid=p=>p+'_'+Math.random().toString(36).slice(2,9);

  const seed={
    route:'home', currentMonth:'2026-07', selectedDate:'2026-07-08',
    page:{record:'home',prep:'home',memory:'home',meal:'list'}, query:'千葉 キャンプ場',
    plan:{title:'スノーピーク赤城山CF',type:'キャンプ',start:'2026-07-07',end:'2026-07-08',place:'スノーピーク赤城山キャンプフィールド',checkin:'13:00',arrivalBuffer:15,home:'千葉県柏市'},
    routePlan:{
      travelMin:210,
      segments:[
        {id:'home',label:'自宅出発',kind:'start',dur:0,stop:0,query:'千葉県柏市'},
        {id:'gas',label:'ガソリン給油',kind:'gas',dur:15,stop:10,query:'ガソリンスタンド'},
        {id:'expressOn',label:'高速に乗る',kind:'road',dur:25,stop:0,query:'高速入口'},
        {id:'rest',label:'SA/PA休憩',kind:'rest',dur:70,stop:15,query:'サービスエリア'},
        {id:'kota1',label:'コタ散歩',kind:'walk',dur:0,stop:15,query:'犬 散歩 休憩'},
        {id:'expressOff',label:'高速を降りる',kind:'road',dur:60,stop:0,query:'高速出口'},
        {id:'conv',label:'通り道コンビニ',kind:'shop',dur:10,stop:10,query:'キャンプ場 近く コンビニ'},
        {id:'super',label:'スーパー',kind:'shop',dur:10,stop:25,query:'キャンプ場 近く スーパー'},
        {id:'tour',label:'観光',kind:'tour',dur:0,stop:30,query:'キャンプ場 周辺 観光'},
        {id:'kota2',label:'コタ散歩',kind:'walk',dur:0,stop:10,query:'キャンプ場 周辺 犬 散歩'},
        {id:'camp',label:'キャンプ場到着',kind:'goal',dur:20,stop:0,query:'スノーピーク赤城山キャンプフィールド'}
      ]
    },
    camp:{active:true,startedAt:'2026-07-08T14:12:00+09:00',endedAt:null},
    activeMode:'',
    events:[{id:'e1',title:'キャンプ',type:'camp',start:'2026-07-07',end:'2026-07-08'},{id:'e2',title:'スノーピーク赤城山CF',type:'camp',start:'2026-07-18',end:'2026-07-20'}],
    gear:[
      {id:'g1',name:'リビングシェル アイボリー',cat:'幕体',box:'home',qty:1,status:'home'},
      {id:'g2',name:'アメニティドームM アイボリー',cat:'幕体',box:'home',qty:1,status:'home'},
      {id:'g3',name:'スキレット大',cat:'料理',box:'kitchen',qty:1,status:'home'},
      {id:'g4',name:'ドッグオフトン',cat:'ペット',box:'pet',qty:1,status:'home'},
      {id:'g5',name:'EcoFlow Glacier',cat:'電源/冷蔵',box:'car',qty:1,status:'home'},
      {id:'g6',name:'シェルコン50',cat:'収納',box:'car',qty:2,status:'home'}
    ],
    shopping:[
      {id:'s1',name:'ブラックタイガー',qty:'1パック',done:false,from:'料理'},
      {id:'s2',name:'にんにく',qty:'1個',done:false,from:'料理'},
      {id:'s3',name:'バター',qty:'1個',done:false,from:'料理'},
      {id:'s4',name:'レモン',qty:'1個',done:false,from:'料理'},
      {id:'s5',name:'氷',qty:'1袋',done:false,from:'保冷'}
    ],
    meals:[
      {id:'m1',name:'ガーリックシュリンプ',slot:'夜',people:2,amount:'ブラックタイガー12〜16尾',gear:['スキレット大','バーナー','トング'],seasoning:['にんにく','バター','オリーブオイル','塩','黒胡椒','レモン'],ingredients:['エビ','にんにく','バター','レモン'],prep:['背わたを取る','水気を拭く','にんにくを刻む'],steps:['スキレットを温める','油とにんにくを入れる','エビを両面焼く','バターとレモンで仕上げる'],note:'バケットなしなら量は控えめで良い。'},
      {id:'m2',name:'ピザ',slot:'夜',people:2,amount:'生地2枚',gear:['スキレット大','アルミホイル','バーナー'],seasoning:['ピザソース','チーズ','オリーブオイル'],ingredients:['ピザ生地','チーズ','具材'],prep:['生地を作る','具材を切る'],steps:['生地を伸ばす','具材を乗せる','弱火で焼く','蓋をしてチーズを溶かす'],note:'生地はリンへLINE共有。'}
    ],
    weather:[{id:'w1',label:'設営時の雨',done:false},{id:'w2',label:'撤収時の雨',done:false},{id:'w3',label:'風速',done:false},{id:'w4',label:'コタの暑さ',done:false}],
    records:[{id:'r1',kind:'開始',title:'自宅出発',time:'14:12',text:'キャンプ開始'},{id:'r2',kind:'終了',title:'コタ散歩終了',time:'14:13',text:'散歩記録'}],
    pins:[{id:'p1',name:'コンビニ候補',kind:'寄り道',x:58,y:50,time:'14:20'},{id:'p2',name:'サイト前',kind:'場所',x:72,y:33,time:'15:10'}],
    track:[{x:18,y:74},{x:27,y:66},{x:34,y:61},{x:44,y:56},{x:57,y:50},{x:66,y:40},{x:72,y:33}],
    gps:{points:3760,gaps:51,distance:12.4,watching:false,current:{x:72,y:33}}, toast:'', undo:null, drawer:null, lastActionAt:0
  };
  let state=load();
  let holdTimer=null, watchId=null;

  function load(){try{const raw=localStorage.getItem(KEY);if(raw)return merge(clone(seed),JSON.parse(raw))}catch(e){}return clone(seed)}
  function merge(a,b){for(const k in b){if(b[k]&&typeof b[k]==='object'&&!Array.isArray(b[k]))a[k]=merge(a[k]||{},b[k]);else a[k]=b[k]}return a}
  function save(){localStorage.setItem(KEY,JSON.stringify(state))}
  function canAct(){const n=Date.now();if(n-state.lastActionAt<550)return false;state.lastActionAt=n;return true}
  function toast(t){state.toast=t;save();render(false);setTimeout(()=>{state.toast='';save();render(false)},1300)}
  function nowText(){return state.camp.active?`キャンプ中 / いま：${state.activeMode||'なし'}`:'まだ始めていない'}
  function currentStatus(){return state.camp.active?'キャンプ中':'未開始'}
  function top(){return `<div class="top"><div class="topRow"><button class="brand" data-go="home"><span class="logo">OB</span><span><b>OUTBASE</b><small>field system</small></span></button><button class="plan" data-go="home"><i class="dot"></i><span><b>${esc(state.plan.title)}</b><small>${fmt(state.plan.start)}〜${fmt(state.plan.end)} / ${esc(nowText())}</small></span></button></div><div class="chips"><span class="on">${esc(currentStatus())}</span><span>いま：${esc(state.activeMode||'なし')}</span><span>${state.gps.watching?'GPS記録中':'GPS待機'}</span></div></div>`}
  function nav(){const items=[['home','⌂','ホーム'],['calendar','▦','予定'],['prep','◫','準備'],['record','＋','記録'],['memory','○','思い出']];return `<nav class="bottomNav">${items.map(([r,i,l])=>`<button class="${state.route===r?'active':''}" data-tab="${r}"><b>${i}</b>${l}</button>`).join('')}</nav>`}
  function shell(html){return `<div class="shell">${top()}<main class="main">${html}</main></div>${drawer()}${state.toast?`<div class="toast">${esc(state.toast)}</div>`:''}${state.undo?`<div class="undo"><span>${esc(state.undo.text)}</span><button data-act="undo">取り消す</button></div>`:''}${nav()}`}

  function hero(label,title,body,badge='今'){return `<section class="hero"><div class="eyebrow"><span>${esc(label)}</span><span>OUTBASE</span></div><h1>${esc(title)}</h1><p>${esc(body)}</p><div class="readout"><span><b>${esc(nowText())}</b><small>画面は簡単。中身は濃く。</small></span><i>${esc(badge)}</i></div></section>`}
  function back(label){return `<div class="back"><button data-back>← ${esc(label)}</button></div>`}
  function titleBox(t,s){return `<section class="title"><b>${esc(t)}</b><small>${esc(s)}</small></section>`}

  function mapCard(label='記録地図', sub='現在地 / 軌跡 / ピン'){
    return `<section class="mapCard"><div class="mapHead"><span><b>${esc(label)}</b><small>${esc(sub)}</small></span><span class="pill">${state.gps.watching?'LIVE':'READY'}</span></div><canvas class="map" id="fieldMap" width="680" height="320"></canvas><div class="mapActions"><button data-act="gps">GPS開始</button><button data-act="pin">ピン</button><button data-act="demoMove">仮移動</button></div></section>`;
  }
  function drawMap(){
    const c=document.getElementById('fieldMap'); if(!c)return;
    const ctx=c.getContext('2d'), w=c.width, h=c.height;
    ctx.clearRect(0,0,w,h);
    ctx.strokeStyle='rgba(246,236,217,.10)'; ctx.lineWidth=1;
    for(let x=0;x<w;x+=42){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,h);ctx.stroke()}
    for(let y=0;y<h;y+=42){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(w,y);ctx.stroke()}
    const pts=state.track.map(p=>[p.x*w/100,p.y*h/100]);
    if(pts.length>1){ctx.strokeStyle='#c6a15d';ctx.lineWidth=6;ctx.lineCap='round';ctx.lineJoin='round';ctx.beginPath();pts.forEach((p,i)=>i?ctx.lineTo(p[0],p[1]):ctx.moveTo(p[0],p[1]));ctx.stroke()}
    state.pins.forEach(p=>{const x=(p.x||50)*w/100,y=(p.y||50)*h/100;ctx.fillStyle='#e6ca8a';ctx.beginPath();ctx.arc(x,y,10,0,Math.PI*2);ctx.fill();ctx.fillStyle='#11140f';ctx.font='bold 16px system-ui';ctx.fillText('●',x-5,y+6);});
    const cur=state.gps.current||state.track[state.track.length-1]||{x:50,y:50}; const x=cur.x*w/100,y=cur.y*h/100;
    ctx.fillStyle='#f6ecd9';ctx.beginPath();ctx.arc(x,y,13,0,Math.PI*2);ctx.fill();ctx.strokeStyle='#c6a15d';ctx.lineWidth=5;ctx.stroke();
    ctx.fillStyle='rgba(246,236,217,.70)';ctx.font='bold 18px system-ui';ctx.fillText('現在地',x+18,y+6);
  }

  function home(){return shell(`<section class="page">${hero('HOME','OUTBASE','キャンプの予定、準備、現地記録、思い出を一本でつなぐ。','BASE')}<section class="section"><div class="head"><span><b>今日やること</b><small>触る場所を迷わない</small></span></div><div class="grid4"><button class="card" data-record-page="camp"><b>キャンプに行く</b><small>出発〜帰宅</small></button><button class="card" data-record-page="kota"><b>コタ散歩</b><small>地図/写真/声</small></button><button class="card" data-go="prep"><b>準備</b><small>出発前に潰す</small></button><button class="card" data-go="memory"><b>思い出</b><small>履歴を見る</small></button></div></section><section class="mini"><button data-go="routeplan">ルート</button><button data-prep-page="meal">料理</button><button data-go="calendar">予定</button></section></section>`)}

  function calendar(){const [y,m]=state.currentMonth.split('-').map(Number),first=new Date(y,m-1,1),start=new Date(first);start.setDate(1-start.getDay());const cells=[];for(let i=0;i<42;i++){const d=new Date(start);d.setDate(start.getDate()+i);cells.push({date:iso(d),inMonth:d.getMonth()===m-1})}return shell(`<section class="page"><section class="calHead"><button data-act="prevMonth">‹</button><div class="calTitle"><b>${y}.${pad(m)}</b><small>2回タップで追加</small></div><button data-act="nextMonth">›</button><button data-act="today">今日</button></section><div class="week">${['日','月','火','水','木','金','土'].map(w=>`<span>${w}</span>`).join('')}</div><div class="days">${cells.map(dayCell).join('')}</div><section class="section"><div class="head"><span><b>予定の中身</b><small>予約、支払い、チェックイン、準備開始をここから見る</small></span></div><div class="list">${state.events.map(e=>`<button class="row"><span><b>${esc(e.title)}</b><small>${fmt(e.start)}〜${fmt(e.end)} / チェックイン ${esc(state.plan.checkin)}</small></span><em>${esc(e.type)}</em></button>`).join('')}</div></section></section>`)}
  function dayEvents(date){return state.events.filter(e=>(e.start||date)<=date&&date<=(e.end||e.start||date)).slice(0,2)}
  function barClass(e,d){if(e.start===e.end)return 'single';if(d===e.start)return 'start';if(d===e.end)return 'end';return 'mid'}
  function dayCell(c){const ev=dayEvents(c.date);return `<button class="day ${c.inMonth?'':'out'} ${c.date===today()?'today':''} ${c.date===state.selectedDate?'selected':''}" data-day="${c.date}"><span class="dayNum">${Number(c.date.slice(8,10))}</span><span class="barWrap">${ev.map(e=>`<i class="eventBar ${barClass(e,c.date)}">${['start','single'].includes(barClass(e,c.date))?esc(e.title):''}</i>`).join('')}</span></button>`}

  function routeSchedule(){
    const [hh,mm]=state.plan.checkin.split(':').map(Number);
    const target=new Date(); target.setHours(hh,mm-state.plan.arrivalBuffer,0,0);
    let total=0; state.routePlan.segments.slice(1).forEach(s=>total+=Number(s.dur||0)+Number(s.stop||0));
    let cur=new Date(target.getTime()-total*60000);
    return state.routePlan.segments.map((s,i)=>{if(i>0)cur=new Date(cur.getTime()+Number(s.dur||0)*60000); const at=new Date(cur); cur=new Date(cur.getTime()+Number(s.stop||0)*60000); return {...s,time:timeFmt(at),leave:timeFmt(cur)}})
  }
  function routePlan(){
    const rows=routeSchedule();
    const way=rows.filter(s=>!['home','camp','expressOn','expressOff'].includes(s.id)).map(s=>s.query).join('|');
    const maps=`https://www.google.com/maps/dir/?api=1&origin=${encodeURIComponent(state.plan.home)}&destination=${encodeURIComponent(state.plan.place)}&travelmode=driving&waypoints=${encodeURIComponent(way)}`;
    return shell(`<section class="page">${back('ホーム')}${hero('ROUTE','チェックイン逆算ルート','給油、休憩、コタ散歩、買い出し、観光まで含めて時間を組む。','13:00')}${mapCard('行程マップ','ルート候補 / ピン / 現在地')}<section class="section"><div class="head"><span><b>行程</b><small>到着目標 ${state.plan.checkin} の${state.plan.arrivalBuffer}分前</small></span><button class="btn main" data-url="${esc(maps)}"><b>Google Maps</b><small>経由地つきで開く</small></button></div><div class="timeline">${rows.map(s=>`<div class="step"><time>${s.time}</time><span><b>${esc(s.label)}</b><small>${s.stop?`${s.stop}分滞在 / `:''}${esc(s.query)}</small></span><em>${esc(s.kind)}</em></div>`).join('')}</div></section><section class="grid2"><button class="btn" data-act="saveRoute"><b>行程保存</b><small>当日ドライブへ使う</small></button><button class="btn dark" data-record-page="drive"><b>ドライブ記録へ</b><small>このまま開始</small></button></section></section>`)
  }

  function prep(){const p=state.page.prep||'home';if(p==='shop')return shopPage();if(p==='load')return loadPage();if(p==='meal')return mealPage();if(p==='weather')return weatherPage();if(p==='gear')return gearPage();return shell(`<section class="page">${back('ホーム')}${hero('PREP','出発前に潰す','買う、積む、作る、走る、見る。準備は作業を終わらせる画面。','準備')}<section class="grid4"><button class="card" data-prep-page="shop"><b>買うもの</b><small>未購入だけ</small></button><button class="card" data-prep-page="load"><b>持っていく</b><small>BOX別に積む</small></button><button class="card" data-prep-page="meal"><b>料理</b><small>材料/手順/ギア</small></button><button class="card" data-go="routeplan"><b>ルート</b><small>チェックイン逆算</small></button><button class="card" data-prep-page="weather"><b>天気</b><small>雨/風/コタ</small></button><button class="card" data-prep-page="gear"><b>ギア</b><small>登録/修理/BOX</small></button></section></section>`)}
  function shopPage(){const undone=state.shopping.filter(s=>!s.done);return shell(`<section class="page">${back('準備')}${titleBox('買うもの','未購入だけ見て、買ったら消す。料理から自動で増える。')}<section class="list">${(undone.length?undone:state.shopping).map(s=>`<button class="row" data-toggle-shop="${s.id}"><span><b>${esc(s.name)}</b><small>${esc(s.qty)} / ${esc(s.from||'')}</small></span><em>${s.done?'済':'まだ'}</em></button>`).join('')}</section><button class="btn dark" data-act="copyShop"><b>LINEコピー</b><small>買い物リストを送る</small></button></section>`)}
  function loadPage(){return shell(`<section class="page">${back('準備')}${titleBox('持っていくもの','今回持っていくギアだけをBOX別に確認。')}<section class="list">${state.gear.map(g=>`<button class="row" data-toggle-gear="${g.id}"><span><b>${esc(g.name)}</b><small>${esc(g.cat)} / BOX:${esc(g.box)}</small></span><em>${g.status==='loaded'?'積んだ':'まだ'}</em></button>`).join('')}</section></section>`)}
  function mealPage(){const m=state.meals[0];return shell(`<section class="page">${back('準備')}${hero('MEAL','料理を薄くしない','人数、量、材料、調味料、必要ギア、下準備、手順、買い物までつなぐ。','料理')}<section class="section"><div class="head"><span><b>${esc(m.name)}</b><small>${m.people}人 / ${esc(m.amount)}</small></span><button class="btn main" data-act="mealToShop"><b>買い物へ</b><small>材料追加</small></button></div><div class="badges">${m.gear.map(x=>`<span class="badge">${esc(x)}</span>`).join('')}${m.seasoning.map(x=>`<span class="badge">${esc(x)}</span>`).join('')}</div></section><section class="subgrid"><section class="section"><div class="head"><span><b>下準備</b><small>出発前にできること</small></span></div><div class="list">${m.prep.map((x,i)=>`<div class="row"><span><b>${i+1}. ${esc(x)}</b><small>前日/当日朝</small></span><em>prep</em></div>`).join('')}</div></section><section class="section"><div class="head"><span><b>当日手順</b><small>現地で迷わない</small></span></div><div class="list">${m.steps.map((x,i)=>`<div class="row"><span><b>${i+1}. ${esc(x)}</b><small>${esc(m.note)}</small></span><em>cook</em></div>`).join('')}</div></section></section><button class="btn dark" data-act="copyMeal"><b>LINEコピー</b><small>リンに送る</small></button></section>`)}
  function weatherPage(){return shell(`<section class="page">${back('準備')}${titleBox('天気判断','設営、撤収、風、気温、コタを判断する。')}<section class="list">${state.weather.map(w=>`<button class="row" data-toggle-weather="${w.id}"><span><b>${esc(w.label)}</b><small>確認したら済みにする</small></span><em>${w.done?'見た':'まだ'}</em></button>`).join('')}</section></section>`)}
  function gearPage(){return shell(`<section class="page">${back('準備')}${titleBox('ギア管理','登録、BOX、乾燥、修理、買い替え。積込とは分ける。')}<section class="grid2"><button class="btn main" data-act="newGear"><b>新しく登録</b><small>ギアを足す</small></button><button class="btn" data-act="newBox"><b>BOX</b><small>収納を登録</small></button></section><section class="list">${state.gear.map(g=>`<div class="row"><span><b>${esc(g.name)}</b><small>${esc(g.cat)} / BOX:${esc(g.box)}</small></span><em>${esc(g.qty)}</em></div>`).join('')}</section></section>`)}

  function record(){const p=state.page.record||'home';if(p==='camp')return campPage();if(p==='drive')return drivePage();if(p==='kota')return walkPage('コタ散歩','帰宅');if(p==='campWalk')return walkPage('場内散歩','サイト帰着');if(p==='site')return sitePage();if(p==='setup')return workPage('設営','設営開始','設営完了');if(p==='withdraw')return workPage('撤収','撤収開始','撤収完了');return recordHome()}
  function recordHome(){return shell(`<section class="page">${back('ホーム')}${hero('FIELD','現地で残す','モードごとに必要なボタンだけ出す。地図あり/なしも分ける。','記録')}<section class="grid4"><button class="card" data-record-page="camp"><b>キャンプ</b><small>出発〜帰宅</small></button><button class="card" data-record-page="drive"><b>ドライブ</b><small>地図あり</small></button><button class="card" data-record-page="kota"><b>コタ散歩</b><small>地図あり</small></button><button class="card" data-record-page="campWalk"><b>場内散歩</b><small>地図あり</small></button><button class="card" data-record-page="site"><b>サイト調査</b><small>地図あり</small></button><button class="card" data-record-page="setup"><b>設営</b><small>地図なし</small></button><button class="card" data-record-page="withdraw"><b>撤収</b><small>地図なし</small></button></section></section>`)}
  function campPage(){return shell(`<section class="page">${back('記録')}${hero('CAMP',state.camp.active?'キャンプ中':'キャンプ開始','自宅出発から帰宅到着までを1本で残す。','親')}<section class="grid2"><button class="btn main" data-act="startCamp"><b>自宅出発</b><small>${state.camp.active?'開始済み':'開始'}</small></button><button class="btn dark" data-record-page="drive"><b>ドライブ</b><small>往路/帰路</small></button><button class="btn" data-act="photo"><b>写真</b><small>今を残す</small></button><button class="btn" data-act="pin"><b>ピン</b><small>場所だけ残す</small></button><button class="btn" data-record-page="setup"><b>設営</b><small>開始/完了</small></button><button class="btn" data-record-page="kota"><b>コタ散歩</b><small>途中散歩</small></button><button class="btn" data-record-page="withdraw"><b>撤収</b><small>乾燥/積込</small></button><button class="btn red" data-safe-end="camp"><b>帰宅した</b><small>長押し終了</small></button></section><section class="info warn">終了系は長押し＋確認。10秒だけ取り消し可能。</section></section>`)}
  function drivePage(){return shell(`<section class="page">${back('記録')}${mapCard('ドライブ地図','現在地 / 軌跡 / ピン / 欠測')}<section class="grid2"><button class="btn main" data-start-mode="ドライブ"><b>出発</b><small>ドライブ開始</small></button><button class="btn dark" data-url="${googleRoute()}"><b>Google Maps</b><small>経由地ルート</small></button><button class="btn" data-act="pin"><b>ピン</b><small>停車中に押す</small></button><button class="btn dark" data-act="voice"><b>話す</b><small>長押し音声</small></button><button class="btn" data-go="routeplan"><b>寄り道</b><small>行程を見る</small></button><button class="btn red" data-safe-end="mode" data-mode="ドライブ"><b>到着</b><small>長押し終了</small></button></section></section>`)}
  function walkPage(title,end){return shell(`<section class="page">${back('記録')}${mapCard(`${title}地図`,'現在地 / 軌跡 / ピン / 写真位置')}<section class="grid2"><button class="btn main" data-start-mode="${title}"><b>出発</b><small>開始</small></button><button class="btn" data-act="pin"><b>ピン</b><small>場所を残す</small></button><button class="btn" data-act="photo"><b>写真</b><small>写真+位置</small></button><button class="btn" data-act="video"><b>動画</b><small>動画+位置</small></button><button class="btn dark" data-act="voice"><b>話す</b><small>音声メモ</small></button><button class="btn red" data-safe-end="mode" data-mode="${title}"><b>${esc(end)}</b><small>長押し終了</small></button></section></section>`)}
  function sitePage(){return shell(`<section class="page">${back('記録')}${mapCard('サイト調査マップ','サイトMAP / ピン / 設備 / 日陰')}<section class="grid2"><button class="btn main" data-start-mode="サイト調査"><b>調査開始</b><small>開始</small></button><button class="btn" data-act="pin"><b>ピン</b><small>場所を残す</small></button><button class="btn" data-act="photo"><b>写真</b><small>サイト写真</small></button><button class="btn dark" data-act="voice"><b>話す</b><small>音声メモ</small></button><button class="btn red" data-safe-end="mode" data-mode="サイト調査"><b>終了</b><small>長押し終了</small></button></section></section>`)}
  function workPage(title,start,end){return shell(`<section class="page">${back('記録')}${hero('WORK',title,'設営/撤収は地図より写真と声。必要ならピンだけ残す。','地図なし')}<section class="grid2"><button class="btn main" data-start-mode="${title}"><b>${esc(start)}</b><small>開始</small></button><button class="btn" data-act="pause"><b>休憩</b><small>一時停止</small></button><button class="btn" data-act="resume"><b>再開</b><small>続き</small></button><button class="btn" data-act="photo"><b>写真</b><small>状況</small></button><button class="btn dark" data-act="voice"><b>話す</b><small>音声</small></button><button class="btn red" data-safe-end="mode" data-mode="${title}"><b>${esc(end)}</b><small>長押し終了</small></button></section></section>`)}

  function memory(){const p=state.page.memory||'home';if(p==='detail')return historyPage();return shell(`<section class="page">${back('ホーム')}${hero('MEMORY','履歴を見る','レビューより先に、ルート・ピン・写真・音声・タイムラインを見返す。','思い出')}<section class="grid4"><button class="card" data-memory-page="detail"><b>今回のキャンプ</b><small>出発〜帰宅</small></button><button class="card" data-memory-page="detail"><b>ルート</b><small>軌跡/欠測</small></button><button class="card" data-memory-page="detail"><b>ピン</b><small>場所一覧</small></button><button class="card" data-memory-page="detail"><b>写真・動画</b><small>記録メディア</small></button><button class="card" data-memory-page="detail"><b>音声</b><small>文字起こし</small></button><button class="card" data-memory-page="detail"><b>改善</b><small>次回に残す</small></button></section><section class="section"><div class="head"><span><b>最近の記録</b><small>押した順に残る</small></span></div><div class="list">${state.records.slice().reverse().map(r=>`<div class="row"><span><b>${esc(r.title)}</b><small>${esc(r.time)} / ${esc(r.text)}</small></span><em>${esc(r.kind)}</em></div>`).join('')}</div></section></section>`)}
  function historyPage(){return shell(`<section class="page">${back('思い出')}${mapCard('詳細履歴マップ','軌跡 / ピン / 写真位置 / 欠測')}<section class="section"><div class="head"><span><b>キャンプ詳細</b><small>GPS ${state.gps.points}点 / 距離 ${state.gps.distance}km / 欠測 ${state.gps.gaps}</small></span></div><div class="list"><div class="row"><span><b>自宅出発</b><small>${new Date(state.camp.startedAt).toLocaleString('ja-JP')}</small></span><em>開始</em></div><div class="row"><span><b>ピン</b><small>${state.pins.map(p=>p.name).join(' / ')}</small></span><em>${state.pins.length}</em></div>${state.records.map(r=>`<div class="row"><span><b>${esc(r.title)}</b><small>${esc(r.time)} / ${esc(r.text)}</small></span><em>${esc(r.kind)}</em></div>`).join('')}</div></section><section class="title"><b>タイムライン</b><small>押した順番で見返す</small></section></section>`)}
  function discover(){return shell(`<section class="page">${back('ホーム')}${titleBox('探す','OUTBASE内、Web、地図、なっぷを分けて探す。')}<input class="input" id="q" value="${esc(state.query)}"><section class="grid2"><button class="btn" data-search="outbase"><b>OUTBASE内</b><small>自分の記録</small></button><button class="btn" data-search="web"><b>Web</b><small>Web検索</small></button><button class="btn" data-search="maps"><b>地図</b><small>Google Maps</small></button><button class="btn" data-search="napp"><b>なっぷ</b><small>キャンプ場</small></button></section><button class="btn main" data-act="saveCandidate"><b>候補保存</b><small>URLを残す</small></button></section>`)}
  function drawer(){if(!state.drawer)return '';if(state.drawer==='event')return `<div class="drawer" data-close><div class="drawerCard" data-stop><h2>予定を足す</h2><label>日付<input id="eventDate" type="date" value="${esc(state.selectedDate)}"></label><label>名前<input id="eventTitle" value="キャンプ"></label><div class="drawerActions"><button class="btn" data-act="closeDrawer"><b>閉じる</b></button><button class="btn main" data-act="saveEvent"><b>保存</b></button></div></div></div>`;if(state.drawer==='gear')return `<div class="drawer" data-close><div class="drawerCard" data-stop><h2>ギア登録</h2><label>名前<input id="gearName"></label><label>カテゴリ<input id="gearCat" value="ギア"></label><div class="drawerActions"><button class="btn" data-act="closeDrawer"><b>閉じる</b></button><button class="btn main" data-act="saveGear"><b>保存</b></button></div></div></div>`;return ''}

  let lastDay='',lastTap=0;
  document.addEventListener('click',e=>{const el=e.target.closest('button,[data-close]'); if(!el)return; if(el.closest('[data-stop]'))return; if(el.dataset.close!==undefined){state.drawer=null;save();return render(false)} if(el.dataset.back!==undefined)return smartBack(); if(el.dataset.tab)return tabGo(el.dataset.tab); if(el.dataset.go)return go(el.dataset.go); if(el.dataset.prepPage){state.route='prep';state.page.prep=el.dataset.prepPage;return navPush()} if(el.dataset.recordPage){state.route='record';state.page.record=el.dataset.recordPage;return navPush()} if(el.dataset.memoryPage){state.route='memory';state.page.memory=el.dataset.memoryPage;return navPush()} if(el.dataset.day){const d=el.dataset.day,n=Date.now(); if(lastDay===d&&n-lastTap<500){state.selectedDate=d;state.drawer='event'}else{state.selectedDate=d;lastDay=d;lastTap=n}save();return render(false)} if(el.dataset.toggleShop){const s=state.shopping.find(x=>x.id===el.dataset.toggleShop);if(s)s.done=!s.done;save();return render(false)} if(el.dataset.toggleGear){const g=state.gear.find(x=>x.id===el.dataset.toggleGear);if(g)g.status=g.status==='loaded'?'home':'loaded';save();return render(false)} if(el.dataset.toggleWeather){const w=state.weather.find(x=>x.id===el.dataset.toggleWeather);if(w)w.done=!w.done;save();return render(false)} if(el.dataset.startMode){state.activeMode=el.dataset.startMode;state.records.push({id:uid('r'),kind:'開始',title:el.dataset.startMode+'開始',time:nowTime(),text:'記録開始'});save();return render(false)} if(el.dataset.url)return window.open(el.dataset.url,'_blank'); if(el.dataset.search)return search(el.dataset.search); if(el.dataset.act)return act(el.dataset.act,el);});
  document.addEventListener('pointerdown',e=>{const el=e.target.closest('[data-safe-end]'); if(!el)return; el.classList.add('holding'); holdTimer=setTimeout(()=>safeEnd(el),1100)});
  ['pointerup','pointercancel','pointerleave'].forEach(ev=>document.addEventListener(ev,e=>{const el=e.target.closest('[data-safe-end]'); if(el)el.classList.remove('holding'); clearTimeout(holdTimer)}));
  function tabGo(r){state.route=r; if(r==='prep')state.page.prep='home'; if(r==='record')state.page.record='home'; if(r==='memory')state.page.memory='home'; navPush()}
  function go(r){state.route=r;navPush()}
  function navPush(){save();try{history.pushState({route:state.route,page:state.page},'',`#${state.route}`)}catch(e){}render(false)}
  function smartBack(){if(state.route==='prep'&&state.page.prep!=='home')state.page.prep='home';else if(state.route==='record'&&state.page.record!=='home')state.page.record='home';else if(state.route==='memory'&&state.page.memory!=='home')state.page.memory='home';else state.route='home';navPush()}
  addEventListener('popstate',()=>smartBack());

  function googleRoute(){const rows=routeSchedule();const way=rows.filter(s=>!['home','camp','expressOn','expressOff'].includes(s.id)).map(s=>s.query).join('|');return `https://www.google.com/maps/dir/?api=1&origin=${encodeURIComponent(state.plan.home)}&destination=${encodeURIComponent(state.plan.place)}&travelmode=driving&waypoints=${encodeURIComponent(way)}`}
  function routeSchedule(){const [hh,mm]=state.plan.checkin.split(':').map(Number);const target=new Date();target.setHours(hh,mm-state.plan.arrivalBuffer,0,0);let total=0;state.routePlan.segments.slice(1).forEach(s=>total+=Number(s.dur||0)+Number(s.stop||0));let cur=new Date(target.getTime()-total*60000);return state.routePlan.segments.map((s,i)=>{if(i>0)cur=new Date(cur.getTime()+Number(s.dur||0)*60000);const at=new Date(cur);cur=new Date(cur.getTime()+Number(s.stop||0)*60000);return {...s,time:timeFmt(at),leave:timeFmt(cur)}})}
  function act(a){if(a==='prevMonth'||a==='nextMonth'){const [y,m]=state.currentMonth.split('-').map(Number),d=new Date(y,m-1+(a==='nextMonth'?1:-1),1);state.currentMonth=`${d.getFullYear()}-${pad(d.getMonth()+1)}`;save();return render(false)}if(a==='today'){state.selectedDate=today();state.currentMonth=today().slice(0,7);save();return render(false)}if(a==='addEvent'){state.drawer='event';save();return render(false)}if(a==='saveEvent'){const d=document.getElementById('eventDate').value,t=document.getElementById('eventTitle').value||'予定';state.events.push({id:uid('e'),title:t,type:'normal',start:d,end:d});state.drawer=null;save();return render(false)}if(a==='closeDrawer'){state.drawer=null;save();return render(false)}if(a==='newGear'){state.drawer='gear';save();return render(false)}if(a==='saveGear'){state.gear.unshift({id:uid('g'),name:document.getElementById('gearName').value||'新しいギア',cat:document.getElementById('gearCat').value||'ギア',box:'home',qty:1,status:'home'});state.drawer=null;save();return render(false)}if(a==='startCamp'){state.camp.active=true;state.camp.startedAt=new Date().toISOString();state.records.push({id:uid('r'),kind:'開始',title:'自宅出発',time:nowTime(),text:'キャンプ開始'});save();return render(false)}if(['photo','video','pin','voice'].includes(a)){if(a==='pin')addPin();state.records.push({id:uid('r'),kind:a==='photo'?'写真':a==='video'?'動画':a==='pin'?'ピン':'音声',title:a==='photo'?'写真':a==='video'?'動画':a==='pin'?'ピン追加':'音声メモ',time:nowTime(),text:'位置付き記録'});save();return toast('記録した')}if(a==='gps')return startGps();if(a==='demoMove')return demoMove();if(a==='saveRoute')return toast('行程を保存');if(a==='mealToShop')return mealToShop();if(a==='copyShop')return copyText(state.shopping.filter(s=>!s.done).map(s=>`・${s.name} ${s.qty}`).join('\n'),'買い物をコピー');if(a==='copyMeal')return copyText('ガーリックシュリンプ\n材料：エビ/にんにく/バター/レモン\n手順：にんにく→エビ→バター→レモン','料理をコピー');if(a==='saveCandidate')return toast('候補に保存');if(a==='pause'||a==='resume')return toast(a==='pause'?'休憩':'再開');if(a==='undo')return runUndo()}
  function safeEnd(el){if(!confirm('終了しますか？'))return;const before=clone(state), type=el.dataset.safeEnd, mode=el.dataset.mode;if(type==='camp'){state.camp.active=false;state.camp.endedAt=new Date().toISOString();state.records.push({id:uid('r'),kind:'終了',title:'帰宅到着',time:nowTime(),text:'キャンプ終了'});state.route='memory';state.page.memory='home'}else{state.records.push({id:uid('r'),kind:'終了',title:(mode||state.activeMode||'記録')+'終了',time:nowTime(),text:'記録終了'});state.activeMode=''}state.undo={text:'終了しました',before};save();render(false);setTimeout(()=>{if(state.undo&&state.undo.text==='終了しました'){state.undo=null;save();render(false)}},10000)}
  function runUndo(){if(state.undo?.before){state=state.undo.before;state.undo=null;save();render(false)}}
  function addPin(){const p=state.gps.current||{x:50,y:50};state.pins.push({id:uid('p'),name:'ピン '+nowTime(),kind:'場所',time:nowTime(),x:p.x,y:p.y})}
  function demoMove(){const last=state.track[state.track.length-1]||{x:50,y:50};const n={x:Math.max(8,Math.min(92,last.x+(Math.random()*14-5))),y:Math.max(10,Math.min(86,last.y+(Math.random()*14-7)))};state.gps.current=n;state.track.push(n);state.gps.points+=1;state.gps.distance=+(state.gps.distance+.08).toFixed(2);save();render(false)}
  function startGps(){state.gps.watching=true; if(navigator.geolocation){watchId=navigator.geolocation.watchPosition(pos=>{const x=20+((pos.coords.longitude*1000)%60),y=20+((pos.coords.latitude*1000)%50);state.gps.current={x,y};state.track.push({x,y});state.gps.points+=1;save();render(false)},err=>{state.gps.gaps+=1;save();toast('位置情報は仮地図で記録')},{enableHighAccuracy:true,maximumAge:5000,timeout:10000})}save();render(false)}
  function mealToShop(){const m=state.meals[0];m.ingredients.forEach(x=>{if(!state.shopping.some(s=>s.name===x))state.shopping.push({id:uid('s'),name:x,qty:'必要量',done:false,from:'料理'})});save();toast('買い物へ追加')}
  function copyText(t,msg){navigator.clipboard?.writeText(t);toast(msg)}
  function search(kind){const q=state.query||'千葉 キャンプ場';if(kind==='outbase')return toast('OUTBASE内を検索');if(kind==='web')open(`https://www.google.com/search?q=${encodeURIComponent(q)}`,'_blank');if(kind==='maps')open(`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(q)}`,'_blank');if(kind==='napp')open(`https://www.nap-camp.com/search?keyword=${encodeURIComponent(q)}`,'_blank')}
  function render(keep=true){const sx=scrollX,sy=scrollY;const r=state.route;app.innerHTML=r==='calendar'?calendar():r==='prep'?prep():r==='record'?record():r==='routeplan'?routePlan():r==='discover'?discover():r==='memory'?memory():home();setTimeout(drawMap,0);if(keep)setTimeout(()=>scrollTo(sx,sy),0)}
  try{history.replaceState({route:state.route,page:state.page},'',`#${state.route}`)}catch(e){}
  render(false);
})();
